import type { Chapter, SectionInfo, SlugMap } from "./generate";

export function resolveMdLinks(
  chapters: Chapter<string>[],
  slugMap: SlugMap,
  sectionInfos: SectionInfo[],
) {
  // TODO: resolve links in md files
}
